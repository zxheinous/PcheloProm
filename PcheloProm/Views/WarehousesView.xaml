<UserControl x:Class="PcheloProm.Views.WarehousesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#F5F6FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Складской учет и текущие остатки сырья/продукции" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Inventories}" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Склад хранения" Binding="{Binding Warehouse.WarehouseName}" Width="1.5*"/>
                <DataGridTextColumn Header="Наименование продукции" Binding="{Binding Product.ProductName}" Width="2*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="110"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding Product.UnitOfMeasure}" Width="80"/>
                <DataGridTextColumn Header="Номер партии" Binding="{Binding BatchNumber}" Width="140"/>
                <DataGridTextColumn Header="Срок годности" Binding="{Binding ExpirationDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>