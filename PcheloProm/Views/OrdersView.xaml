<UserControl x:Class="PcheloProm.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#F5F6FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Журнал заказов и продаж «ПчелоПром»" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер заказа" Binding="{Binding OrderNumber}" Width="130"/>
                <DataGridTextColumn Header="Клиент / Контрагент" Binding="{Binding Customer.FullName}" Width="2*"/>
                <DataGridTextColumn Header="Дата оформления" Binding="{Binding OrderDate, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Width="150"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding OrderStatus}" Width="110"/>
                <DataGridTextColumn Header="Оплата" Binding="{Binding PaymentStatus}" Width="120"/>
                <DataGridTextColumn Header="Итоговая сумма" Binding="{Binding TotalAmount, StringFormat=\{0:C\}}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>