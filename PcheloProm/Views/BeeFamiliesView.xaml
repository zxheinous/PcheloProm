<UserControl x:Class="PcheloProm.Views.BeeFamiliesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#F5F6FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Учет биологических единиц (Пчелиные семьи)" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding BeeFamilies}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер семьи" Binding="{Binding FamilyNumber}" Width="120"/>
                <DataGridTextColumn Header="Порода" Binding="{Binding Breed}" Width="150"/>
                <DataGridTextColumn Header="Возраст матки (мес)" Binding="{Binding QueenAge}" Width="130"/>
                <DataGridTextColumn Header="Сила семьи" Binding="{Binding Strength}" Width="100"/>
                <DataGridTextColumn Header="Статус здоровья" Binding="{Binding HealthStatus}" Width="150"/>
                <DataGridTextColumn Header="Рейтинг" Binding="{Binding ProductivityRating}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>