<Window x:Class="PcheloProm.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Авторизация — ПчелоПром" Height="450" Width="350" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#F5F6FA" CornerRadius="8" BorderThickness="1" BorderBrush="#DCDDE1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#2C3E50" Height="40">
                <TextBlock Text="Авторизация системы" VerticalAlignment="Center" Margin="15,0,0,0" 
                           Foreground="White" FontSize="14" FontWeight="SemiBold"/>

                <Button Content="✕" HorizontalAlignment="Right" Width="40" Height="40" 
                        Background="Transparent" Foreground="White" BorderThickness="0" 
                        FontSize="16" FontWeight="Bold" Cursor="Hand" Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E74C3C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <StackPanel Grid.Row="1" Margin="30,40,30,30" VerticalAlignment="Top">

                <TextBlock Text="🐝 ПЧЕЛОПРОМ" Foreground="#F1C40F" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Информационная система хозяйства" Foreground="#7F8C8D" FontSize="11" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <TextBlock Text="Имя пользователя (Логин):" Foreground="#34495E" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUsername" Height="35" FontSize="14" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,20">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="Пароль:" Foreground="#34495E" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="35" FontSize="14" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,0,10">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>

                <TextBlock x:Name="lblError" Text="" Foreground="#E74C3C" FontSize="12" Margin="0,0,0,15" HorizontalAlignment="Center" FontWeight="SemiBold"/>

                <Button Content="Войти в систему" Height="40" Background="#16A085" Foreground="White" 
                        FontSize="15" FontWeight="Bold" Cursor="Hand" Click="LoginButton_Click" Margin="0,10,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1ABC9C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>